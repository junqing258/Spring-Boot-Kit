<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.inlighting.mapper.UserMapper">

    <resultMap id="UserResultMap" type="org.inlighting.entity.UserEntity" >
        <id column="user_id" property="userId" jdbcType="INTEGER" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="role" property="role" jdbcType="VARCHAR" />
        <result column="permission" property="permission" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="mobile" property="mobile" jdbcType="VARCHAR" />
    </resultMap>

    <!--<select id="selectUserById" resultMap="UserResultMap" parameterType="long" >
        select * from users where user_id=#{id,jdbcType=INTEGER}
    </select>-->

    <select id="selectUserByName" resultType="org.inlighting.entity.UserEntity" parameterType="String" >
    	<![CDATA[
       	select * from users where username=#{username}
        ]]>
    </select>

    <!--<insert id="insertUser" resultMap="java.lang.Long" parameterType="org.inlighting.entity.UserEntity"
        useGeneratedKeys="true" keyProperty="user_id" keyColumn="user_id">
         insert into users (username, password, role, permission) VALUES (#{username}, #{password}, #{role}, #{permission})
    </insert>-->

</mapper>